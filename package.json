{
  "name": "traceable-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A Claude skill for building internal backends a non-coder owner can verify from a built-in DevOps UI.",
  "license": "Apache-2.0",
  "scripts": {
    "test": "node scripts/self-test.mjs",
    "example:drift": "node scripts/check-drift.mjs examples/reference-app"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "pg": "^8.13.1",
    "yaml": "^2.6.1"
  }
}
